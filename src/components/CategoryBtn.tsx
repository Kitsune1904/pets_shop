/*
import {ReactNode} from "react";
import * as classes from "./App.scss";

const CategoryBtn = ({categories}):ReactNode => {
    const handleClick = (event) => {
        const clickedBtn:string = event.target.id;
    }

    return (
        <div className={classes.categoriesHolder}>
            {categories.map((category, index) => <button id={`${category}Btn`} onClick={handleClick}>{category}</button>)}
        </div>
    )
};

export default CategoryBtn*/
